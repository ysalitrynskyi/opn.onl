# react-helmet-async@2 hasn't updated its peer range to include React 19, but it
# works fine with it. Allow installs (npm ci / npm install, local + CI + Docker)
# to proceed instead of failing on the strict peer-dependency check.
legacy-peer-deps=true
